# Optional training stack — install in a separate venv (Python 3.10–3.12 recommended).
#   cd backend && python3.13 -m venv .venv-train && .venv-train/bin/pip install -r requirements-train.txt
#
# Requires NVIDIA GPU + CUDA for local QLoRA. Use the Colab notebook if this fails.

torch>=2.4.0
transformers>=4.44.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.9.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Unsloth (optional — faster training; install separately if pip wheel matches your CUDA):
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
